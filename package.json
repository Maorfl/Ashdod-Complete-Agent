{
  "name": "caspi-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Caspi Agent — מערכת אוטומציה פנימית לשחרור מטענים בנמל אשדוד והעברה לחיפה (ה.כספי)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test:pipeline": "npm run test:pipeline -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
